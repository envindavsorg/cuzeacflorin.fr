export const WHITESPACE_SEPARATOR: RegExp = /\s+/;

export const CONSECUTIVE_DIGITS_PATTERN = /\d+/;

export const WHITESPACE_AND_HYPHEN_SEPARATOR: RegExp = /[\s-]+/;

export const FRONTMATTER_REGEX = /^---\s*([\s\S]*?)\s*---/;

export const RAW_REGEX = /^['"](.*)['"]$/;
